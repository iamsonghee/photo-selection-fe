import { SystemLoadingScreen } from "@/components/SystemLoadingScreen";

/** 고객 token 내부 페이지의 streaming/navigation fallback. */
export default function CustomerTokenLoading() {
  return <SystemLoadingScreen />;
}
