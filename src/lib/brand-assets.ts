/**
 * 파비콘 등에 쓰는 정사각 마크(SVG). 파일 교체 시 `BRAND_MARK_REV`만 올리면
 * 브라우저 캐시를 함께 끊을 수 있습니다.
 */
export const BRAND_MARK_REV = "1";
export const BRAND_MARK_SVG = `/brand/a-cut-mark.svg?rev=${BRAND_MARK_REV}`;
